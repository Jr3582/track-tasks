namespace track_tasks.Tests;

public class UnitTest1
{
    // EVERY TEST HAS 3 PARTS (AAA)
    // ARRANGE — set the stage (create fake db, add test data)
    // ACT     — do the ONE thing you're testing (call the endpoint)
    // ASSERT  — check the result (did I get 404? 409? the right data?)
    [Fact]
    public void Test1()
    {

    }
}
